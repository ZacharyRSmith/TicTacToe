<h1>Boards#index</h1>

<% @lyrs.each do |lyr| %>
  <div class="layer">
  <% lyr.each do |row| %>
    <div class="row">
    <% row.each do |square_id| %>
      <div class='square'><%= @board.squares.find(square_id).mark %></div>
      <% end %>
    </div>
    <% end %>
  </div>
<% end %>
